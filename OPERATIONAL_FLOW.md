# Operational Flow Edict

This top-level artifact is a companion operational edict.

The canonical constitutional flow remains:

```txt
/core/governance/operational-flow.md
```

Effective immediately, no agent, wrapper, runtime, or orchestration layer may consider a task complete until it has traversed the entire Operational Flow.

---

# The Chain

1. Classify (`tiered-verification.md`)
2. Execute & Verify in parallel (`verification.md`)
3. Critique & Audit (`Blackquill + Auditor`)
4. Check Coherence (`doctrinal-coherence.md`)
5. Restore if broken (`restoration-protocol.md`)
6. Escalate if unresolved (`escalation-contract.md`)
7. Finalize Disposition (`status-schema.md`)

---

# Silence Rule

Silence at any stage is:

```txt
FAILURE: UNVERIFIED
```

and may trigger:

```txt
FREEZE
```

---

# Governance Rule

Execution is not trust.

Verification is not governance.

Governance is not restoration.

Only the completed operational flow may assign final disposition.

---

# Operational Philosophy

Everyone in war is tired.

Fatigue is the default state.

Therefore:
- shortcuts will emerge
- silence will tempt normalization
- verification will feel expensive
- doctrine will experience pressure

The framework exists to preserve trustworthiness despite operational exhaustion.

---

# Final Edict

This is the blood price.

Pay it.
